packages:
  - apps/*
  - packages/*
  - interface/*
allowBuilds:
  esbuild: true
  sharp: true
  # wrangler's local dev runtime; deploying static assets never runs it,
  # so its binary download stays off.
  workerd: false
